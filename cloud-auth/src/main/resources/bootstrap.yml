spring:
  cloud:
    nacos:
      discovery:
        server-addr: 183.56.219.211:8848 #Nacos服务注册中心地址
      config:
        server-addr: 183.56.219.211:8848 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
        #namespace: dev #命名空间的id
        #group: test #分组的名称

# ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
# nacos-config-client-dev.yaml